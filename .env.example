# Server-side secrets (never expose these to the browser — no VITE_ prefix).
# Get a free key at https://build.nvidia.com → pick any model → "Get API Key"
NVIDIA_API_KEY=nvapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
